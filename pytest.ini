[pytest]
testpaths = tasks/api/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    integration: tests that hit the FastAPI TestClient
    slow: long-running tests
