"""Реестр коннекторов. Импорт модуля регистрирует площадку автоматически.

В списке — ТОЛЬКО те площадки, с которых мы реально забираем закупки.
Диагностические заглушки убраны 04.08.2026 по решению владельца: площадка,
которая ничего не приносит, только засоряет админку.

Что и почему НЕ подключено (замеры 31.07 и 04.08):

  ЕИС (zakupki.gov.ru) — коннектор написан и работает, но отвечает только
    из домашней сети: датацентровые адреса блокируются, не помогает даже
    отключение проверки TLS. Владелец отказался от домашнего сборщика
    («гос тендеры не нужны»), поэтому `eis.py` оставлен на диске, но НЕ
    импортируется. Включить — раскомментировать строку ниже и поднять
    сборщик с TR_LOCATION=home.

  ТЭК-Торг — API найден (`/api/getProcedures`), отдаёт JSON, но параметры
    постраничности вскрыть не удалось: перебраны page, p, pageNumber,
    offset, start, currentPage, limit, perPage, pageSize и вложенные формы
    — эндпоинт всегда возвращает первые 10 записей из двух миллионов.
    Забирать 10 свежих — это видимость охвата, а не охват.

  ТендерПро — публичного списка закупок нет, всё за авторизацией.

  Росэлторг — сайт открывается, витрину закупок найти не удалось.

  Фабрикант (403), РТС-тендер (503), Сбербанк-АСТ (не отвечает вовсе),
    ЗаказРФ (пустая витрина, к тому же госзаказ Татарстана) — закрыты
    либо мимо профиля.

  ГИС Торги (torgi.gov.ru) — имущественные торги, не наш профиль.
"""
from __future__ import annotations

from .base import (  # noqa: F401
    BaseSource,
    HttpClient,
    RawTender,
    SourceRequiresAuth,
    SourceUnavailable,
    all_sources,
    get_source,
    register,
)

# Порядок импорта = порядок появления площадок в админке.
from . import b2b_center  # noqa: F401,E402
from . import bidzaar  # noqa: F401,E402
from . import etp_gpb  # noqa: F401,E402

# from . import eis  # noqa: F401,E402  — только из домашней сети, см. шапку
