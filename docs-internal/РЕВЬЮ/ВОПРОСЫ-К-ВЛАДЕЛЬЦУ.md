# Вопросы к владельцу (нужны для точного ревью)

> По коду видно КАК работает, но не всегда ЖИВ ли модуль и НУЖЕН ли эндпоинт.
> Ответы превращают «подозрения» в конкретные вердикты (удалить / закрыть / оставить).

## 1. Периметр (определяет severity ВСЕГО блока безопасности)

**Портал `portalsth.ru` доступен из открытого интернета или закрыт?**
(VPN / IP-allowlist в nginx / basic-auth на уровне сервера / только по паролю в приложении)

→ Если **открыт наружу** — десятки пишущих эндпоинтов без авторизации становятся
🔴 (любой может грузить контакты, чистить кэш, удалять чаты/проекты, а
`admin/vosk-upload` — писать файлы на VPS). Если **за периметром** — это осознанный
компромисс, severity падает до 🟢.

## 2. Живость модулей (для категории 🗑/❓)

По каждому — **используется ли сейчас** (да / изредка / умер):
- **validator** (веб-анкета кандидата с голосом, Vosk на VPS) — жив? От него зависят `/validator/*` и `/admin/vosk-*`
- **recruiter** — статическая версия и/или Streamlit (`recruiter/app.py`)?
- **analyst, chat, month, hr-game, kp, sales, wb, finance, okk** — какие реально открываются, какие заброшены?

## 3. Легаси — можно ли убирать (для 🗑)

- `/ai/url`, `ai_url.json`, `start-ai.bat`, ngrok-упоминания — точно мертвы? (сейчас Ollama статична через SSH-туннель)
- `scripts/inject_*.py` (5 шт) — одноразовые миграции HTML, отработали?
- `voicecall/local_bot.py`, `test_call.py`, `test_register.py` — нужны для ручной отладки или в утиль?
- `/admin/analyst-recipes` — legacy-алиас, старый фронт ещё зовёт?

## 4. Приоритеты правок

Какой P0/P1 берём первым, когда перейдём к правкам:
бэкапы БД · закрытие открытых эндпоинтов · escHtml/XSS · pytest в CI?

---

**Как удобнее:** можешь ответить прямо тут по пунктам, или рассказать голосом/текстом
«что зачем нужно» — я разнесу по находкам и уточню вердикты (удалить/закрыть/оставить).
