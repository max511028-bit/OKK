# Находки код-ревью (review-only, ничего не правим)

> Категории и severity — см. `ПЛАН-РЕВЬЮ.md`. Каждая правка — отдельной задачей
> ПОСЛЕ согласования с владельцем. Ничего в этом заходе не меняется.
> Обновляется по мере прохождения блоков.

Легенда: 🗑 удалить · 🔧 починить · ➕ доработать · ♻️ рефакторинг · ❓ проверить
· severity 🔴🟠🟢

---

## Блок 1 — Безопасность ✅

### Хорошее (подтверждено, не трогать)
- SQL — параметризован везде; 2 f-string execute (`main.py:242,282`) — это `ALTER TABLE`
  с именами колонок из **литерального** списка, не из ввода. Безопасно.
- Path traversal вложений закрыт `_safe_name()` (`main.py:457`): basename + regex +
  обрезка — на upload/download/delete. ОК.
- Нет `bare except:` в бэкенде.

### Находки

| ID | Тег | Sev | Место | Что / почему | Рекомендация |
|---|---|---|---|---|---|
| B1-1 | 🔧 | 🔴 | `main.py` (глобально) | Пишущих эндпоинтов ~64, с явной проверкой пароля/токена ~37 → часть POST/PUT/DELETE **без авторизации**. Часть осознанно (dispatch/* — свой пароль внутри; «внутренний портал за паролем»), но нужно поимённо подтвердить, что нет случайно открытых мутаций | Составить матрицу «эндпоинт → уровень доступа», закрыть непреднамеренно открытые. Блок 2 добьёт список |
| B1-2 | 🔧 | 🟠 | фронты: `voicecall/index.html` (42×`innerHTML`), `okk` (58), `tasks` (37), `wb` (32) | Данные (имена кандидатов, тексты сценариев, ответы) вставляются через `innerHTML`. Если в имени/тексте есть HTML — XSS. Риск низкий (внутр. инструмент, свои данные), но кандидатские имена/расшифровки — внешний ввод | Проверить, что каждый `innerHTML` с внешними данными идёт через `escHtml()`. Составить список «сырых» вставок |
| B1-3 | 🔧 | 🟠 | `main.py` (авторизация) | Пароли-дефолты (`PORTAL_PASSWORD`/`ADMIN_PASSWORD`) как фолбэк в коде → значения в git-истории (уже в 10-ТЕХДОЛГ №2) | Убрать дефолты, только env. **Пароли не трогаем по решению владельца — оставляю как находку** |
| B1-4 | ❓ | 🟢 | `main.py:767` download_attachment | Комментарий «Открытый эндпоинт — портал за паролем». Файлы задач качаются без токена (по имени файла + tid) | Подтвердить, что портал целиком за периметром; иначе — закрыть токеном |

---

## Блок 2 — Данные и надёжность бэкенда ⏳ (частично)

### Хорошее
- Доступ к БД обёрнут (`with db()` 75×) — единый контекст соединения.

### Находки
| ID | Тег | Sev | Место | Что / почему | Рекомендация |
|---|---|---|---|---|---|
| B2-1 | 🔧 | 🔴 | VPS `tasks.db` | **Нет бэкапов боевой БД** (вся история обзвонов/задач). Дубль 10-ТЕХДОЛГ №1 | Ежедневный авто-бэкап VPS→ПК по ssh |
| B2-2 | 🔧 | 🟠 | `candidate_validations.verdict` NOT NULL | Реальная ловушка: UPDATE в NULL падал (ловили при переклассификации). Есть и другие NOT NULL в старых таблицах | Аудит всех NOT NULL vs места записи NULL |
| B2-3 | ➕ | 🟢 | миграции в `init_db()` | Идемпотентны (try/except), но безоткатны и без версии схемы. При росте — боль | Опционально: таблица `schema_version` |

## Блок 3 — Голосовой агент ✅

### Хорошее (боевой, обкатанный)
- Фоновые потоки — `daemon=True` (recheck, prewarm), утечек нет.
- Очистка ресурсов: `call.hangup`/`phone.stop` в `finally` (15 мест), `duration_s` тоже.
- Сетевые вызовы: `_rpc` дефолтный `timeout=30`, зависаний нет.
- 5 патчей pyVoIP — с подробными комментариями-обоснованиями.

### Находки
| ID | Тег | Sev | Место | Что / почему | Рекомендация |
|---|---|---|---|---|---|
| B3-1 | ♻️ | 🟠 | `phone_call.py` (34×), `dispatch_agent.py` (18×) `except Exception: pass/…` | Много «best effort»-глушений. Большинство осознанны, но при таком количестве легко спрятать реальный баг | Аудит списком: каждый — залогировать причину (многие уже логируют), убедиться что ни один не глотает критичное |
| B3-2 | ➕ | 🟢 | таймаут недозвона ~45с | Долго/дорого (эфир Novofon). Дубль 10-ТЕХДОЛГ №8 | После «соединения по речи» резать до ~30с |
| B3-3 | ❓ | 🟢 | `_STT_RECHECK_BUSY = Semaphore(1)` | Пере-распознавание сериализовано (1 в момент), чтобы не забивать CPU. Но при длинной кампании очередь rechecks может копиться | Проверить, не растёт ли backlog на больших базах |

## Блок 4 — Фронтенды ✅ (систематические находки; поштучно — при правках)

### Находки
| ID | Тег | Sev | Место | Что / почему | Рекомендация |
|---|---|---|---|---|---|
| B4-1 | 🔧 | 🟠 | 8 из 12 фронтов: `tasks, okk, wb, finance, hr-game, analyst, chat, month` | **Нет функции `escHtml`** — данные вставляются в `innerHTML` без эскейпинга (есть только в voicecall/kp/recruiter/validator). XSS на любых динамических данных | Ввести единый `escHtml`, обернуть вставки внешних данных. Приоритет — где данные внешние (кандидаты, CSV) |
| B4-2 | ♻️ | 🟠 | все фронты | Огромные монолиты (analyst 5651, tasks 5468, finance 3771...), ~31к строк суммарно, **ноль переиспользования** — AI-чат/пароль/утилиты копипастятся через inject-скрипты | Вынести общий `portal-common.js` (escHtml, fetch-обёртка, AI-виджет). Крупный рефакторинг, по желанию |
| B4-3 | ➕ | 🟢 | все фронты | Vanilla JS без сборки, нет минификации — файлы по 3000+ строк грузятся как есть | Приемлемо для внутр. инструмента; при росте — сборка |
## Блок 5 — Мёртвый код и легаси ⏳ (первый проход)

| ID | Тег | Sev | Место | Что / почему | Рекомендация |
|---|---|---|---|---|---|
| B5-1 | 🗑 | 🟠 | `main.py` (ngrok ×9), `/ai/url`, `ai_url.json`, `start-ai.bat` | Наследие ngrok-эры: адрес Ollama давно статичен (127.0.0.1:21434 через SSH-туннель), механизм записи URL мёртв | Проверить, что `/ai/url` никем не вызывается, затем вычистить |
| B5-2 | 🗑 | 🟢 | `scripts/inject_*.py` (5 шт), `scripts/fix-cloudflared.ps1` | Одноразовые скрипты-инжекторы (правили HTML при старой схеме) и cloudflared-фиксы | Проверить неиспользуемость → архив/удаление |
| B5-3 | 🗑 | 🟢 | `voicecall/local_bot.py`, `test_call.py`, `test_register.py` | Ручные отладочные скрипты (не тесты pytest) | Пометить как debug/ или убрать |
| B5-4 | 🗑 | 🟢 | `/admin/analyst-recipes` (GET+POST) | Легаси-алиасы: указывают на те же функции, что `/admin/analyst-canon` | Оставить алиасы ради старых фронтов ИЛИ убрать после проверки фронта |
| B5-5 | 🗑 | 🟢 | `recruiter/app.py` (Streamlit), задача `STH-Cloudflared-Tunnel` (disabled), `voicecall/PLAN_DORABOTOK.md` | Легаси-версии/устаревшее | Проверить → убрать |
| B5-6 | ❓ | 🟠 | `scripts/install-ai-watchdog.ps1`, `start-ai-tunnel.ps1` | Незакоммиченные чужие правки в рабочей копии (по договорённости НЕ коммитить). Чьи и нужны ли — неясно | Разобраться с владельцем, зафиксировать статус |
## Блок 6 — Инфраструктура/деплой ✅

| ID | Тег | Sev | Место | Что / почему | Рекомендация |
|---|---|---|---|---|---|
| B6-1 | 🔧 | 🔴 | нет бэкапов `tasks.db` | см. B2-1 | ежедневный бэкап |
| B6-2 | 🔧 | 🟠 | CI не гоняет pytest | push с красными тестами доедет до прода | добавить шаг pytest в `deploy.yml` (блокирующий) |
| B6-3 | 🗑 | 🟠 | осиротевшая SYSTEM-задача туннеля | вхолостую перезапускает ssh ежеминутно (безвредна). Дубль 10-ТЕХДОЛГ №4 | вычистить от админа |
| B6-4 | ♻️ | 🟢 | два вотчдога (`ai-watchdog.ps1` SYSTEM + `sth-ai-watchdog.ps1` user) пишут в один лог | путаница, конфликт лога (B2/техдолг №6,7) | консолидировать в один |
| B6-5 | 🟢 | 🟢 | VPS 1 vCPU/960МБ впритык | нет запаса; при росте нагрузки — потолок | мониторить, план апгрейда |

## Блок 7 — Тесты ✅

### Хорошее
- ~236 тестов, каждый боевой баг закрыт регресс-тестом с датой-случаем.
- Сеть/LLM мокаются; правило «тесты только в tests/» соблюдается.

### Находки
| ID | Тег | Sev | Место | Что / почему | Рекомендация |
|---|---|---|---|---|---|
| B7-1 | 🔧 | 🟠 | `test_ai_cache.py::TestStreamReplay::test_replay_yields_chunks_and_final` | Стабильно красный: `_ai_cache_stream_replay` стал async-генератором, тест итерирует синхронно | Поправить тест (await/anyio). Есть спавн-задача |
| B7-2 | ➕ | 🟢 | покрытие | Не покрыты: SIP/RTP-тракт, фронты (0 JS-тестов), CI/деплой | Для SIP — только живой звонок; фронты — по желанию |

## Блок 8 — Итог и приоритеты

### Общая оценка
Система **боевая и на удивление здоровая** для своего объёма: SQL параметризован,
path traversal закрыт, ресурсы агента чистятся, каждый баг покрыт тестом. Основные
риски — **не в логике, а в эксплуатации**: нет бэкапов, дыры в авторизации/эскейпинге,
накопившийся легаси.

### Топ-приоритеты (что чинить первым, КОГДА дашь команду)

| Приоритет | Находки | Почему |
|---|---|---|
| 🔴 P0 | B2-1/B6-1 (бэкапы БД) | одна потеря диска = вся история обзвонов/задач |
| 🔴 P0 | B1-1 (аудит открытых пишущих эндпоинтов) | случайно открытая мутация = порча данных |
| 🟠 P1 | B4-1 (escHtml в 8 фронтах), B1-2 (XSS) | внешние данные (кандидаты) без эскейпинга |
| 🟠 P1 | B6-2 (pytest в CI) | защита от регрессий на проде |
| 🟡 P2 | B5-* (вычистка легаси: ngrok, inject, дубли) | снижает шум и порог входа |
| 🟡 P2 | B3-1 (аудит swallowed exceptions), B6-3/4 (зомби-задача, два вотчдога) | скрытые сбои, чистота |
| 🟢 P3 | B4-2 (общий portal-common.js), B2-3 (schema_version), B3-2 (таймаут) | качество/удобство, не срочно |

### Что НЕ трогать (обосновано)
- 5 патчей pyVoIP, монолит main.py (пока не болит), vanilla-JS без сборки,
  SQLite вместо Postgres, поллинг вместо push — все с причинами в 01-АРХИТЕКТУРА.

### Дальнейшие шаги
1. Поштучный аудит B1-1 (матрица эндпоинт→доступ) и B4-1 (список сырых innerHTML) —
   отдельным проходом, если нужно детальнее.
2. Правки — строго отдельными задачами по согласованию, начиная с P0.

